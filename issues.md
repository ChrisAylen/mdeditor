# Required Features

## To Do

- Local AI through ollama to allow for automated formatting and text generation.

## Future Ideas - Do not implement yet



## Done

- The MD reader should reflect formatting.  right now it just displays the raw markshon.  The application should have a split panel design, on the left show the raw markdown as it does now and on the right, show the markdown  corretly formatted (e.g. Title corretly shoen in bold, correct size etc.)
- Saving a new file should add .md suffix automatically
- Implement numbered lists: Using numbers instead of bullets results in a displayed bullet, not a numberd list.  Currently, "1. Test list"  dispalys as "- Test list"  This should result in a numbered list.
- implement task lists correctly (e.g. - [ ] Incomplete task, - [x] Complete task - This still does not work
- implement table support (renders correctly via markdown `extra` extension)
- Formatting menu for users to select / insert items like headings, lists, tables, bold, italic, and horizontal rules

