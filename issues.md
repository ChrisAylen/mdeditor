# Required Features

## To Do

- Use local AI through Ollama to automate formatting and generate text.

## Future Ideas (Not for implementation yet)

- Local AI through ollama to allow for automated formatting and text generation (implemented — see below)

## Done

- Local AI using Ollama facilitates automated formatting and text generation.

| Feature | Description |
|---|---|
| MD Reader Formatting | The MD reader should reflect formatting. Currently it just displays the raw markdown. The application should have a split‑panel design: the left panel shows the raw markdown as it does now, and the right panel shows the markdown correctly formatted (e.g., a title shown in bold, correct heading sizes, etc.). |
| Save New File Suffix | When saving a new file, the application should automatically add the `.md` suffix. |
| Numbered Lists | Implement numbered lists correctly. At present, a line like ``1. Test list`` is rendered as ``- Test list`` (a bullet). It should render as a numbered list. |
| Task Lists | Implement task lists correctly (e.g., ``- [ ] Incomplete task`` and ``- [x] Complete task``). These are currently not functioning. |
| Table Support | Implement table support so that tables render correctly via the Markdown `extra` extension. |
| Formatting Menu | Provide a formatting menu that lets users select/insert items such as headings, lists, tables, bold, italic, and horizontal rules. |




